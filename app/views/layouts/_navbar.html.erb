<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">WhoBrings</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="<%= 'active' if current_page?(root_path) || current_page?(root_path) %>"><%= link_to 'Home', root_path %></li>
        <li class="<%= 'active' if current_page?(user_events_path(current_user.id)) %>"><%= link_to 'Events', user_events_path(current_user.id) %></li>
      </ul>
     </ul>
      
      <ul class="nav navbar-nav navbar-right">
        <li class="<%= 'active' if current_page?(about_path) %>"><%= link_to 'About', about_path %></li>
            <% if (!current_user) %>
              <li><%= link_to "Log in with Facebook", "/auth/facebook" %></li>
             <!--  <li><a href="/sign_up">Sign Up</a></li> -->
            <%  else  %> 
              <li><%= link_to "Logout", "/logout" %><li>
            <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>




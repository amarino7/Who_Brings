<%= render "layouts/navbar" %>

<div>
	<%= @task.content %><br>
	<%= @task.user_id %><br>
	<%= @task.cost %><br>
	<%= @task.complete %><br>


	<%= button_to "Delete", user_event_task_path(@current_user.id, @event.id, @task.id), method: :delete %>
	<%= button_to 'Edit', edit_user_event_task_path(@current_user.id, @event.id, @task.id ), :method => "get" %>


</div>


